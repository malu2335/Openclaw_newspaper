# AGENTS.md

## Cursor Cloud specific instructions

This repository (`Openclaw_newspaper`) is currently an empty project with only an MIT LICENSE file. There is no application code, no dependency manifests, no configuration, and no services to run.

### Current state (as of initial setup)

- **No language runtime required** — no source code exists yet.
- **No package manager** — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar files.
- **No services** — nothing to start, build, lint, or test.
- **No Docker / database dependencies**.

### When code is added

Once the project has actual source code, this section should be updated with:
- How to install dependencies
- How to run lint, tests, and the dev server
- Any non-obvious environment caveats
